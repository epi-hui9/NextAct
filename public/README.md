# Public assets

- `icons/` PWA icons
- `illustrations/` optional brand art
- `sw.js` service worker
- `offline.html` offline shell

Manifest is generated by `src/app/manifest.ts` → `/manifest.webmanifest`.
