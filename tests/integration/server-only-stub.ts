// Test stub for the `server-only` marker package, which throws when imported
// outside a React Server Component. Under Vitest we alias it to this no-op so
// server modules (adapters, retrieval) can be unit tested directly.
export {};
